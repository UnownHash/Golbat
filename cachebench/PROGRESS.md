# cachebench progress log

Append-only log, one line per completed step (per the investigation brief's
"Reporting back" contract).

- 2026-07-06: Brief read; CLAUDE.md caching/locking/eviction sections reviewed. Hardware for all benchmarks: Apple M3 Pro, 12 cores, 36GB RAM, go1.26.3 darwin/arm64 (NOTE: production is 48+ cores / 360GB — 96-goroutine runs oversubscribe 12 cores here).
- 2026-07-06: cachebench module scaffolded (separate go.mod, module golbat/cachebench). Deps pinned: ttlcache v3.4.1 (repo uses v3.4.0), otter v2.3.0, theine-go v0.6.2, xsync v4.5.0.
- 2026-07-06: Local API audit of otter v2.3.0 done (go doc + source): unbounded mode OK (MaximumSize unset); per-entry TTL only via custom ExpiryCalculator (no ttl arg on Set) — adapter wraps values with a ttl field; ComputeIfAbsent = single-winner creation; OnDeletion delivered via Executor whose DEFAULT is `go fn()` (goroutine per event — same shape as ttlcache fact 1) but Executor is injectable and OnAtomicDeletion runs inline on the maintenance path; 1s periodic cleanup goroutine per cache; deletion causes distinguish Invalidation/Expiration/Overflow/Replacement.
- 2026-07-06: Local API audit of theine v0.6.2 done: NewBuilder REQUIRES maxsize (bounded W-TinyLFU); Set/SetWithTTL return bool (admission can REJECT a write); per-entry TTL via SetWithTTL; no touch-on-hit on read (expiry fixed at write; touch = full re-Set); RemovalListener called synchronously (no per-event goroutine) with REMOVED/EVICTED/EXPIRED reasons; no per-call GetOrSetFunc equivalent (loader fixed at build time); timer wheel + 1s maintenance ticker.
- 2026-07-06: Upstream audit (ttlcache latest/issues + otter/theine docs) delegated to background research agent; results land in RECOMMENDATION.md.
- 2026-07-06: Upstream/API research agent returned. Headlines: ttlcache v3.4.1 fixes NONE of facts 1-4 (inline-callback PR #190 rejected; only fix = cleaner wake on TTL shorten, PR #206); otter v2.3.0 supports unbounded TTL-only mode + per-entry TTL via ExpiryCalculator but default OnDeletion = goroutine per event + open drainBuffers contention issue #177; theine v0.6.2 disqualifying as drop-in (mandatory max size, Set can decline (doorkeeper) or be undone by async admission — open issue #3, listener inline on maintenance goroutine with BLOCKING writer sends). Full detail goes in RECOMMENDATION.md.
- 2026-07-06: Adapters written for all 4 candidates (ttlcache = faithful decoder/sharded_cache.go replica; otter = ExpiryCalculator wrapper + OnAtomicDeletion; theine = 2x-headroom bounded + touch-emulation-by-reSet + striped-mutex GetOrSetFunc emulation; proto = thin wrapper).
- 2026-07-06: protocache prototype built (~420 lines): xsync.Map storage, atomic per-entry deadline, lazy expiry on Get (never returns expired), striped timing wheel with bucket-ownership dedupe (wheelIdx), single-dispatcher bounded-queue callbacks. 14 tests green under -race (expiry correctness, touch semantics, single-winner, callback once/order/reasons, supersede-silently, concurrent mixed-op hammer).
- 2026-07-06: Brief update ingested: added ttlcache-hyst candidate (hysteresis touch, mirrors decoder commit 75b3df0: threshold=TTL/4, re-jittered refresh) to benches 1/2/6; decision rule now judged against it. Appendix confirmations queued for RECOMMENDATION.md: point 3 (replacement-cause filtering) pinned by TestReplacementFiresNoEviction (all candidates PASS under -race); point 5 (full-cache Range at 10M) measured in TestScenarioMemory.
- 2026-07-06: Benchmarks 1/2/4/6 written (bench_test.go), scenarios 3/5/7 written (scenario_test.go). Smoke runs at small N pass; full-scale orchestration in run.sh (per-candidate process isolation for 10M cases). Official run starting.
- 2026-07-06: Official full-scale run launched (run.sh -> results/, N=10M, per-candidate processes). Correctness suite PASS under -race at full candidate set. RECOMMENDATION.md drafted with complete upstream/API-fit audits + appendix confirmations; decision-rule verdict pending numbers. collect_results.py assembles RESULTS.md from raw logs.
- 2026-07-06: Full-scale run round 1 complete at N=10M (benchmarks 1/2/4/6 all candidates + shard sensitivity; scenarios 3/5/7). Three measurement defects found and fixed: (a) wave steady-state baseline was GC-contaminated by the 11GB preload — now GC-flushed + sampled only in the last 30s pre-wave; (b) churn GC never triggered at GOGC=100 vs 11GB live heap — now forced with SetGCPercent(5); (c) added TestScenarioCallbackBomb (blocking callbacks under mass expiry = the ed50bf8 incident shape, with a writer-latency probe). Reruns of wave/mem/bomb in flight.
- 2026-07-06: Round-1 headline numbers (vs ttlcache-hyst baseline, 10M entries): read-hot g96 otter 3.3x / proto 2.0x / theine 1.9x; touch otter 4.9x / proto 5.4x / theine 0.3x (its re-Set emulation is 3.3x SLOWER); storm otter/proto/theine all >3.6x; single-goroutine consumer ~parity (alloc-dominated); memory: every candidate under ttlcache's 1231 B/entry (mapref floor 1054); Range 10M: theine 0.48s < proto 0.60s < otter 1.39s < ttlcache 1.56s.
- 2026-07-06: Scenario reruns complete with fixed measurement. Wave (post-fix): ttlcache steady p99 174µs is GENUINE (not GC contamination) — its worst case is full-cache steady state, not the wave; proto/otter steady p99 2.7/3.7µs. Forced-GC churn: GC CPU proto 2.7s < otter 3.5s < ttlcache 5.8s < theine 6.7s.
- 2026-07-06: Bomb scenario (two arms) complete: ttlcache parks ~200k goroutines under BOTH a 10s consumer stall and a chronic 100µs-slow consumer (the ed50bf8 incident reproduced); proto 7, otter 19, theine 18. theine blocks cache WRITERS for 6.97s in the slow arm (maintenance-goroutine backpressure) — decode-path invariant violation demonstrated.
- 2026-07-06: RESULTS.md assembled (collect_results.py; tables + raw logs). RECOMMENDATION.md finalized: REPLACE — prototype and otter pass all decision-rule gates vs the neutralized (hysteresis) baseline; theine fails perf gates and is API-disqualified; recommendation = prototype first (exact semantics, safe-by-default delivery, best GC/memory), otter fallback with mandatory executor + cause-filter guardrails; ttlcache unfixed upstream (bump to v3.4.1 regardless). Investigation COMPLETE.
